<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="gr.qpc.meteoclimaandroid.MeteoclimaSettingsActivity">

    <TextView android:text="@string/widget_interval_setting_label" android:layout_width="wrap_content"
        android:id="@+id/widget_interval_label"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:textSize="20dp"
        android:textAlignment="center" />
    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="number"
        android:ems="10"
        android:id="@+id/widget_interval_value"
        android:textSize="16dp"
        android:text="23"
        android:layout_below="@+id/widget_interval_label"
        android:layout_centerHorizontal="true"
        android:textAlignment="gravity"
        android:gravity="center_horizontal" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Save"
        android:id="@+id/save_button"
        android:layout_below="@+id/widget_interval_value"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
